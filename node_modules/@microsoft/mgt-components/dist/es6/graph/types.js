/**
 * -------------------------------------------------------------------------------------------
 * Copyright (c) Microsoft Corporation.  All Rights Reserved.  Licensed under the MIT License.
 * See License in the project root for license information.
 * -------------------------------------------------------------------------------------------
 */
const viewTypes = ['image', 'oneline', 'twolines', 'threelines', 'fourlines'];
export const isViewType = (value) => {
    return typeof value === 'string' && viewTypes.includes(value);
};
export const viewTypeConverter = (value, defaultValue = 'twolines') => {
    if (isViewType(value)) {
        return value;
    }
    return defaultValue;
};
//# sourceMappingURL=types.js.map